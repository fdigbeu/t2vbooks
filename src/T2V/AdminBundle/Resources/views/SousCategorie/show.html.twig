{% extends 'base.html.twig' %}

{% block body %}

	<div>{{ render(controller('T2VAdminBundle:Menu:index', {'active': app.request.get('_route')})) }}</div>
	
	<div class="container">

    <div class="page-header">
    	<h3 class="text-center">Information du sous-titre</h3>
    </div>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sousCategorie.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ sousCategorie.titre }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ sousCategorie.numero }}</td>
            </tr>
        </tbody>
    </table>

    <ul class="t2v_adminbundle">
        <li>
            <a class="btn btn-lg btn-info btn-block" href="{{ path('souscategorie_index') }}">Revenir à la liste</a>
        </li>
        <li>
            <a class="btn btn-lg btn-primary btn-block" href="{{ path('souscategorie_edit', { 'id': sousCategorie.id }) }}">Modifier</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input class="btn btn-lg btn-danger btn-block" type="submit" value="Supprimer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
    
    </div>
{% endblock %}
