{% extends 'base.html.twig' %}

{% block body %}

	<div>{{ render(controller('T2VAdminBundle:Menu:index', {'active': app.request.get('_route')})) }}</div>
	
	<div class="container">

    <div class="page-header">
    	<h3 class="text-center">Afficher tous les titres</h3>
    </div>

    <table class="table">
        <thead>
            <tr>
                <th>Titre du livre</th>
                <th>Numéro</th>
                <th>Titre du sommaire</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie in categories %}
            <tr>
                <td>{{ categorie.livre.titre }}</td>
                <td>{{ categorie.numero }}</td>
                <td>{{ categorie.titre }}</td>
                <td>
                    <a class="btn btn-info btn-xs" href="{{ path('categorie_show', { 'id': categorie.id }) }}">Afficher</a>
                    <a class="btn btn-primary btn-xs" href="{{ path('categorie_edit', { 'id': categorie.id }) }}">Modifier</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul class="t2v_adminbundle">
        <li>
            <a class="btn btn-lg btn-primary btn-block" href="{{ path('categorie_new') }}">Ajouter un nouveau titre</a>
        </li>
    </ul>
    
    </div>
{% endblock %}
